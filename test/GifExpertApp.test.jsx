import { render, screen } from "@testing-library/react";
import { GifExpertApp } from "../src/GifExpertApp";

describe("pruebas en GifExpertApp", () => {
  test("renders the component with initial category", () => {
    
  });
});
